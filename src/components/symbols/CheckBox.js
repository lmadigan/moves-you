import React from 'react';
import Icon from 'react-native-vector-icons/FontAwesome';

const CheckBox = ({ size }) => {
  return (
    <Icon name="check" size={size} color='#007aff' />
  );
};
export { CheckBox };
