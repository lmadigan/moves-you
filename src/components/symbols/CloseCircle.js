import React from 'react';
import Icon from 'react-native-vector-icons/Ionicons';

const CloseCircle = ({ color }) => {
  return (
    <Icon name="ios-close-circle" size={25} color={color} />
  );
};
export { CloseCircle };
